export class Carga {
    // Constructor: se llama cuando se instancia la clase
    constructor(carga,coulumb,vector) {
     this.carga = carga;
     this.coulumb = coulumb;
     this.vector = vector;
}
} 